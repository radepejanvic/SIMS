<Window x:Class="Library.View.Form.HospitalRefferalFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:Library.View"
        xmlns:viewmodel="clr-namespace:Library.ViewModel.Form" 
        d:DataContext="{d:DesignInstance Type=viewmodel:HospitalRefferalFormViewModel}"
        mc:Ignorable="d" 
        Title="Uput za bolnicko lecenje" Height="480" Width="480">

    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition  />
            <RowDefinition  />
            <RowDefinition  />
            <RowDefinition  Height="auto" />
            <RowDefinition  />
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock
                Grid.Row="0"
                Text="Uput za bolnicko lecenje"
                Style="{StaticResource Header}" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="FirstColumn"/>
                <ColumnDefinition SharedSizeGroup="SecondColumn"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Text="Trajanje lecenja:" />
            <TextBox
                Grid.Column="1" Margin="0,0,0,10"
                Text="{Binding Duration}"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="FirstColumn"/>
                <ColumnDefinition SharedSizeGroup="SecondColumn"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Text="Potrebne dodatne analize: "
                Margin="0 0 15 0"/>
            <CheckBox
                Grid.Column="1"
                IsChecked="{Binding AdditionalAnalysis}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0 0 0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="FirstColumn"/>
                <ColumnDefinition SharedSizeGroup="SecondColumn"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Text="Spisak lekova: "
                Margin="0 0 15 0"/>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <ListView Grid.Row="0" Height="100" ItemsSource="{Binding SelectedDrugs}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Lekovi" Width="Auto"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Grid Grid.Row="1">
                    <StackPanel Orientation="Horizontal">
                        <ComboBox 
                            ItemsSource="{Binding AvaliableDrugs}" 
                            SelectedItem="{Binding SelectedDrug, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                            DisplayMemberPath="Name"
                            Margin="10"
                            Width="110"/>
                        <Button Content="Dodaj" Command="{Binding AddDrug}"/>
                    </StackPanel>
                </Grid>

            </Grid>
        </Grid>

        <Grid Grid.Row="4">
            <StackPanel 
                Grid.Column="2" 
                Orientation="Horizontal"  
                HorizontalAlignment="Right" 
                VerticalAlignment="Center">
                <Button Content="Potvrdi"
                        Margin="10 0 10 0"
                        Command="{Binding CreateHospitalRefferal}"/>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
