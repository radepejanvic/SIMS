<Window x:Class="Library.View.Form.PatientAddNotificationFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View.Form"
        mc:Ignorable="d"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="PatientAddNotificationFormView" Height="400" Width="480">
    <Grid RenderTransformOrigin="0.561,0.504">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock 
                Grid.Column="0"
                FontSize="24"
                Text="Dodavanje notifikacije"
                Style="{StaticResource Header}"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20 20 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Vreme : "
                Margin="0 0 15 0"/>

            <xctk:TimePicker 
                Grid.Column="1"
                Format="Custom" x:Name="fromDTP"
                FormatString="HH:mm"
                Text="{Binding Time}" />
        </Grid>

        <Grid Grid.Row="2" Margin="20 10 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Datum pocetka: "
                Margin="0 0 15 0"/>

            <DatePicker 
                Grid.Column="1"
                SelectedDate="{Binding From, Mode=TwoWay}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="20 10 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Datum kraja : "
                Margin="0 0 15 0"/>

            <DatePicker 
                Grid.Column="1"
                SelectedDate="{Binding To, Mode=TwoWay}"/>
        </Grid>

        <Grid Grid.Row="4" Margin="20 10 10 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock 
                Grid.Column="1"
                FontSize="14"
                Margin="20 0 0 0"
                Text="Poruka notifikacije:"
                VerticalAlignment="Center"
                />

            <TextBox Grid.Column="1" 
                Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"  
                Margin="147,18,-87,18"
                Height="100"
                Width="200"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto" RenderTransformOrigin="0.652,0.493"/>
        </Grid>

        <Grid Grid.Row="7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <Button 
                Grid.Column="0"
                Margin="50 30 10 10"
                Padding="7"
                Content="Dodaj"
                Background="#fff"
                Command="{Binding AddNotificationCommand}"
               />
            <Button 
                Grid.Column="1"
                Margin="60 30 10 10"
                Padding="7"
                Content="Izadji"
                Background="#fff"
                Command="{Binding CloseCommand}"
                CommandParameter="{Binding ElementName=myWindow}"
                />
        </Grid>
    </Grid>
</Window>
