<Window x:Class="Library.View.Form.RoomRenovationFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View.Form"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="Zahtev za renoviranje sobe" Height="350" Width="300">
    <Grid>
 
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock 
                Grid.Column="0"
                FontSize="20"
                Text="Zahtev za renoviranje sobe"
                Style="{StaticResource Header}"/>
        </Grid>
        <StackPanel Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                
                <TextBlock Text="Izberi te sobu:" Grid.Row="0" Margin="115,0,5,10"/>
                <ComboBox Width="150" Height="20" Grid.Row="1"  ItemsSource="{Binding Rooms}" SelectedItem="{Binding SelectedRoom}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding RoomIDandType}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <TextBlock Text="Izberi te kog ce tipa sada bit soba:" Grid.Row="0" Margin="60,0,5,10"/>
                <ComboBox Width="150" Height="20" Grid.Row="1" ItemsSource="{Binding Types}" SelectedItem="{Binding SelectedType}"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="3" Margin="30 10 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Datum od: "
                Margin="40 0 15 0"/>

            <DatePicker 
                Grid.Column="1"
                SelectedDate="{Binding Begin, Mode=TwoWay}"/>
        </Grid>
        <Grid Grid.Row="4" Margin="30 10 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="Datum do: "
                Margin="40 0 15 0"/>

            <DatePicker 
                Grid.Column="1"
                SelectedDate="{Binding End, Mode=TwoWay}"/>
        </Grid>

        <Button Grid.Row="5" Width="80" Height="30"  Content="Dodaj"  FontFamily="Arial Rounded MT Bold" Command="{Binding AddRoomRenovationCommand}"/>

    </Grid>
</Window>
