<Window x:Class="Library.View.Table.LeaveRequestTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View.Table"
        mc:Ignorable="d"
        Title="LeaveRequestTableView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="350" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" Margin="5 10 5 5" ItemsSource="{Binding LeaveRequest}" SelectedItem="{Binding SelectedLeaveRequest}" Width="700" BorderBrush="Black">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" >
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"  />
                    <Setter Property="Padding" Value="5" />

                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View >
                <GridView>


                    <GridViewColumn Header="Ime" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding Doctor}" Padding="0 0 20 0" Width="auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="OD"  >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding From}" Padding="0 0 20 0" Width="auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="DO"  >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding To}" Padding="0 0 0 0" Width="auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    
                    <GridViewColumn Header="Razlog"  >
                        <GridViewColumn.CellTemplate >
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding LeaveReason}" Padding="0 0 0 0" Width="auto"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition  />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Width="110" Height="30"  Content="Prihvati"  FontFamily="Arial Rounded MT Bold" Command="{Binding AcceptLeaveRequestCommand}"/>
                <Button Grid.Column="1" Width="110" Height="30"  Content="Odbi"  FontFamily="Arial Rounded MT Bold" Command="{Binding NotAcceptLeaveRequestCommand}"/>

            </Grid>
        </StackPanel>
    </Grid>
</Window>
