<Window x:Class="Library.View.Table.Survey.DoctorSurveyTableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library.View.Table.Survey"
        mc:Ignorable="d"
        Title="DoctorSurveyTableView" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <ListView  Grid.Column="0" Margin="5 10 5 5" ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor}" Width="150" Height="200" BorderBrush="Black">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" >
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"  />
                                <Setter Property="Padding" Value="5" />

                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View >
                            <GridView>
                                <GridViewColumn Header="Ime"  Width="auto">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding FullName}" Padding="0 0 20 0" Width="auto"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Width="200" Height="40" Grid.Column="1" Content="Prikaz izvestaj za doktora" Command="{Binding ShowDoctorSurveyAlalitics}"/>
                </Grid>
            </StackPanel>
            <StackPanel Grid.Row="1">
                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition  Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <ListView Grid.Column="0" Margin="5 10 5 5" ItemsSource="{Binding DoctorSurveys}" SelectedItem="{Binding SelectedDoctorSurvey}" Width="80" Height="150" BorderBrush="Black">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" >
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"  />
                                <Setter Property="Padding" Value="5" />

                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View >
                            <GridView>
                                <GridViewColumn Header="Id anketa"  Width="auto">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock HorizontalAlignment="Center" Text="{Binding Id}" Padding="0 0 20 0" Width="auto"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <Button Grid.Column="1" Width="200" Height="40" Content="Prikaz izvestaj za doktora" Command="{Binding ShowDoctorSurvey}" />
                </Grid>
            </StackPanel>

        </Grid>
    </Grid>
</Window>
